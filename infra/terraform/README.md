# Terraform stubs (Phase 3)

This folder is ready for provider modules:

- `modules/vercel` — web app project
- `modules/fly` — api + worker apps
- `modules/cloudflare` — DNS / CDN
- `modules/postgres` — managed Postgres

No credentials are committed. Wire remote state before applying.
