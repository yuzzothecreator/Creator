# Ecommerce API

Python FastAPI e-commerce backend at [`apps/ecommerce-api`](../apps/ecommerce-api).

It is a separate service from the NestJS Creator platform API (`apps/api`). See the app README for run instructions, env vars, Alembic, and demo credentials.
